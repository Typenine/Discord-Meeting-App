# Frontend Environment Variables

# ============================================================================
# REQUIRED FOR PRODUCTION DEPLOYMENT
# ============================================================================
# Cloudflare Worker domain (required for production)
# Used for:
#   - WebSocket connections in Standalone mode
#   - HTTP API requests in Discord Activity mode (if VITE_API_BASE not set)
# Example: discord-agenda-activity-worker.yourusername.workers.dev
# DO NOT include protocol (https:// or wss://) - it will be added automatically
VITE_WORKER_DOMAIN=

# ============================================================================
# OPTIONAL: Alternative API Base Configuration
# ============================================================================
# Override API base URL for HTTP polling mode
# If not set, will use https://${VITE_WORKER_DOMAIN}/api
# Only needed if your API is on a different domain than VITE_WORKER_DOMAIN
# VITE_API_BASE=https://custom-domain.com/api

# ============================================================================
# LOCAL DEVELOPMENT
# ============================================================================
# For local development, you can leave these empty.
# The app will automatically use:
#   - ws://localhost:8787/api/ws for WebSocket
#   - http://localhost:8787/api for HTTP API
# 
# To test with a deployed worker locally:
# VITE_WORKER_DOMAIN=your-worker.workers.dev
